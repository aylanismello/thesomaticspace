---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import { sessionTypes } from '../content/data.js';
---

<BaseLayout
  title="Sessions | The Somatic Space"
  description="Detailed information about somatic sessions: point-holding and regulation work for nervous system integration."
>
  <main class="sessions-page">
    <!-- Header -->
    <section class="page-header">
      <div class="header-background">
        <img
          src="https://images.unsplash.com/photo-1517495306984-f84210f9daa8?q=80&w=987&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
          alt=""
        />
      </div>

      <Header currentPath="/sessions" />

      <div class="container">
        <h1>Sessions</h1>
        <p class="header-description">
          Working with the body's innate capacity for regulation, integration, and healing.
        </p>
      </div>
    </section>

    <!-- Session Details -->
    <section class="session-details">
      <div class="container">
        {sessionTypes.map((sessionType, index) => (
          <div class="session-detail-card">
            <h2>{sessionType.name}</h2>
            <p class="session-description">{sessionType.description}</p>

            <div class="session-info">
              {index === 0 ? (
                <>
                  <h3>What to Expect</h3>
                  <p>
                    In non-touch sessions, you remain seated and clothed. We work through verbal guidance,
                    attention tracking, and somatic awareness. The focus is on noticing sensation,
                    building capacity, and supporting the nervous system to regulate without force or manipulation.
                  </p>
                  <p>
                    Sessions move at your pace. There's no agenda beyond what emerges in real time.
                    This work is about presence, not protocol.
                  </p>
                </>
              ) : (
                <>
                  <h3>What to Expect</h3>
                  <p>
                    Point-holding sessions take place on a massage table. You remain clothed in comfortable layers.
                    Touch is gentle, sustained, and non-invasive—used as a form of contact and containment,
                    not manipulation. The practitioner's hands follow the body's internal cues in stillness.
                  </p>
                  <p>
                    These sessions support parasympathetic settling and allow the nervous system to soften
                    in the presence of safe, attuned contact. The work is slow and respectful of your system's pace.
                  </p>
                </>
              )}
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Rates -->
    <section class="rates-section">
      <div class="container">
        <h2>Rates</h2>
        <div class="rates-grid">
          <div class="rate-card">
            <h3>Somatic Point-Holding<br />(Touch-Based)</h3>
            <p class="rate-price">$200</p>
            <p class="rate-duration">90 minutes</p>
          </div>
          <div class="rate-card">
            <h3>Somatic Regulation & Integration<br />(Non-Touch)</h3>
            <p class="rate-price">$180</p>
            <p class="rate-duration">90 minutes</p>
          </div>
        </div>
        <p class="rates-note">
          Sessions are offered in-person only. Sliding scale available on request.
        </p>
      </div>
    </section>

    <!-- Inquire Section -->
    <section id="inquire" class="inquire-section">
      <div class="inquire-background">
        <img
          src="https://images.unsplash.com/photo-1511207538754-e8555f2bc187?q=80&w=927&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
          alt=""
        />
      </div>

      <div class="container">
        <div class="content-medium">
          <h2 class="text-white">Inquire</h2>

          <p class="text-large text-light inquire-intro">
            If you're interested in working together, send a brief note about what you're looking for and your general availability. I'll respond with next steps, rates, and scheduling options.
          </p>

          <form
            name="inquiry"
            method="POST"
            action="/success"
            data-netlify="true"
            netlify-honeypot="bot-field"
            class="inquiry-form"
          >
            <input type="hidden" name="form-name" value="inquiry" />
            <p class="hidden">
              <label>
                Don't fill this out if you're human: <input name="bot-field" />
              </label>
            </p>

            <div>
              <label for="name">Name</label>
              <input type="text" id="name" name="name" required />
            </div>

            <div>
              <label for="email">Email</label>
              <input type="email" id="email" name="email" required />
            </div>

            <div>
              <label for="session-type">Preferred session type</label>
              <select id="session-type" name="session-type" required>
                <option value="">Select an option</option>
                <option value="non-touch">Non-touch</option>
                <option value="point-holding">Point-holding</option>
                <option value="not-sure">Not sure</option>
              </select>
            </div>

            <div>
              <label for="message">Message</label>
              <textarea
                id="message"
                name="message"
                required
                placeholder="Tell me a bit about what you're looking for..."
              ></textarea>
            </div>

            <button type="submit" class="submit-button">
              Send inquiry
            </button>
          </form>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="page-footer">
      <div class="container">
        <div class="footer-content">
          <p class="font-serif">© The Somatic Space — Francesco Mello</p>
          <p class="italic font-serif">
            This work is not psychotherapy or medical treatment.
          </p>
        </div>
      </div>
    </footer>
  </main>
</BaseLayout>

<style>
  @import '../styles/global.css';

  .sessions-page {
    min-height: 100vh;
    background-color: var(--color-cream);
  }

  /* Header */
  .page-header {
    position: relative;
    min-height: 40vh;
    display: flex;
    align-items: center;
    padding: 8rem 0 4rem;
  }

  .header-background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .header-background::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.7) 0%, rgba(0, 0, 0, 0.4) 50%, rgba(0, 0, 0, 0.6) 100%);
    z-index: 1;
  }

  .header-background img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .page-header .container {
    position: relative;
    z-index: 10;
  }

  .page-header h1 {
    margin-bottom: 1.5rem;
    color: white;
  }

  .header-description {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.9);
    max-width: 65ch;
  }

  /* Session Details */
  .session-details {
    padding: 5rem 0;
  }

  .session-detail-card {
    margin-bottom: 4rem;
    padding-bottom: 4rem;
    border-bottom: 1px solid var(--color-accent-light);
  }

  .session-detail-card:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .session-detail-card h2 {
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    margin-bottom: 1.5rem;
    color: var(--color-text-primary);
  }

  .session-description {
    font-size: 1.125rem;
    line-height: 1.7;
    margin-bottom: 2.5rem;
    color: var(--color-text-secondary);
  }

  .session-info {
    max-width: 68ch;
    margin: 0 auto;
  }

  .session-info h3 {
    font-size: 1.25rem;
    margin-bottom: 1.25rem;
    margin-top: 2rem;
    text-align: center;
  }

  .session-info p {
    margin-bottom: 1.25rem;
    text-align: left;
    line-height: 1.7;
  }

  /* Rates Section */
  .rates-section {
    padding: 5rem 0 6rem;
    background-color: var(--bg-sand);
  }

  .rates-section h2 {
    margin-bottom: 3rem;
  }

  .rates-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    max-width: 56rem;
    margin: 0 auto 2.5rem;
  }

  @media (min-width: 768px) {
    .rates-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2.5rem;
    }
  }

  .rate-card {
    background: white;
    padding: 2.5rem 2rem;
    text-align: center;
    border: 1px solid var(--color-accent-light);
  }

  .rate-card h3 {
    font-size: 1.25rem;
    margin-bottom: 1.5rem;
    line-height: 1.4;
    color: var(--color-text-primary);
  }

  .rate-price {
    font-size: 2.5rem;
    font-weight: 600;
    color: var(--color-accent);
    margin-bottom: 0.5rem;
  }

  .rate-duration {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .rates-note {
    text-align: center;
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    max-width: 50ch;
    margin: 0 auto;
  }

  /* Inquire Section */
  .inquire-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 5rem 0;
  }

  .inquire-background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .inquire-background::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.7) 0%, rgba(0, 0, 0, 0.4) 50%, rgba(0, 0, 0, 0.6) 100%);
    z-index: 1;
  }

  .inquire-background img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .inquire-section .container {
    position: relative;
    z-index: 10;
  }

  .content-medium {
    max-width: 56rem;
    margin: 0 auto;
  }

  .text-white {
    color: white;
    text-align: center;
    margin-bottom: 2rem;
  }

  .text-large {
    font-size: 1.125rem;
  }

  .text-light {
    color: rgba(255, 255, 255, 0.9);
  }

  .inquire-intro {
    text-align: center;
    margin-bottom: 3rem;
  }

  .inquiry-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .inquiry-form label {
    display: block;
    margin-bottom: 0.5rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: white;
  }

  .inquiry-form input,
  .inquiry-form textarea,
  .inquiry-form select {
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.3);
    width: 100%;
    padding: 0.75rem 1rem;
    font-size: 1rem;
    font-family: inherit;
  }

  .inquiry-form input:focus,
  .inquiry-form textarea:focus,
  .inquiry-form select:focus {
    background: white;
    border-color: white;
    outline: none;
  }

  .inquiry-form textarea {
    min-height: 150px;
    resize: vertical;
  }

  .submit-button {
    width: 100%;
    background: white;
    color: var(--color-text-primary);
    border: 2px solid white;
    font-weight: 600;
    padding: 1rem 2rem;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 1rem;
  }

  .submit-button:hover {
    background: transparent;
    color: white;
  }

  @media (min-width: 640px) {
    .submit-button {
      width: auto;
    }
  }

  .hidden {
    display: none;
  }

  /* Footer */
  .page-footer {
    padding: 3.5rem 0;
    background-color: var(--bg-cream);
    border-top: 1px solid var(--color-accent-light);
  }

  .footer-content {
    text-align: center;
  }

  .footer-content > * + * {
    margin-top: 0.75rem;
  }

  .footer-content {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  .font-serif {
    font-family: var(--font-family-serif);
  }

  .italic {
    font-style: italic;
  }
</style>
